import Home from './pages/Home'

/**
 * Application root.
 * Currently renders the Home (chat) page directly.
 * Add a router here when more pages are introduced.
 */
export default function App() {
  return <Home />
}
